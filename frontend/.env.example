# URL base da API usada pelo frontend Vite.
# Use "/api" quando frontend e backend estiverem no mesmo dominio via proxy.
# Use "https://api.exemplo.local/api" quando o backend estiver em outro host.
VITE_API_URL=/api
